# Анализ эффективности компаний (N & M Metrics)

Программа на Python для расчета и визуализации динамики затрат и эффективности продаж на основе исторических данных из CSV-файла. Софт анализирует финансовые показатели компаний в интервале с 2008 по 2025 год.

📌 Описание задачи
Скрипт считывает базу данных компаний и рассчитывает два ключевых параметра для оценки финансового состояния бизнеса:
1. **Параметр N (Прирост затрат)**: Отражает изменение абсолютных издержек компании (разница между выручкой и прибылью) год к году.
2. **Параметр M (Коэффициент эффективности)**: Показывает отношение прироста выручки к приросту затрат. Позволяет понять, сколько дополнительной выручки приносит каждая единица вложенных затрат.

🧮 Математическая модель
Для каждой пары последовательных лет ($t-1$ и $t$) рассчитываются следующие показатели:
• Затраты (Costs): $Costs = Sales - Profit$
• Ось X (N): 
$$N = (Sales_t - Profit_t) - (Sales_{t-1} - Profit_{t-1})$$
• Ось Y (M): 
$$M = \frac{Sales_t - Sales_{t-1}}{N}$$

🚀 Функционал
• **Гибкий выбор**: Пользователь вводит название фирмы вручную.
• **Динамический диапазон**: Фильтрация данных в интервале 2008–2025 гг.
• **Обработка ошибок**: Защита от деления на ноль и отсутствующих данных за конкретные года.
• **Визуализация**: Построение интерактивного графика (Scatter Plot с линиями тренда) с подписями периодов.

🛠 Требования и Установка
Для работы скрипта требуются библиотеки `pandas` (опционально) и `matplotlib`.

1. Клонируйте репозиторий:
```bash
git clone https://github.com/vash-login/company-efficiency-analysis.git
```
2. Установите зависимости:
```bash
pip install matplotlib
```
3. Подготовьте данные:
Разместите ваш файл `export_dataset.csv` в папке, указанной в переменной `FOLDER_PATH` внутри скрипта. Файл должен иметь следующие столбцы:
`rank, name, country, sales, profit, asset, market_value, year, id`.

📊 Пример работы
После запуска программы введите название компании. Вы получите таблицу вида:

| Пара лет | N (Затраты) | M (Эффект) |
|----------|-------------|------------|
| 2018-2019| 1500.00     | 1.2540     |
| 2019-2020| -200.00     | 0.8500     |

И график траектории развития компании:

---
**Built with ❤️ for financial transparency and risk management**